# Contributing

1. Use synthetic credentials in code, tests, screenshots, and issue reports.
2. Run `dotnet test KeyHub.slnx -c Release` before opening a pull request.
3. Keep project manifests free of secret values and personal absolute paths.
4. Do not weaken host fingerprint verification or restart-command allowlists without an explicit design discussion.
5. Update README and tests when changing CLI or manifest behavior.
